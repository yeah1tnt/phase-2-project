import React from "react";

function HomePage(){
    return (
        <div>
            <h1>Home Page</h1>
            <h3>Instruction for using this page:</h3>
            <p>Enter a new word to add it into the data base<br></br>
                if the word is already the database, the word will not be added
            </p>
        </div>
    )
}

export default HomePage;